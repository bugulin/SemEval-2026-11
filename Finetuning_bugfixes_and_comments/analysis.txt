The training was more or less correct, however, the line 
	model = peft.get_peft_model(model, peft_config)
wrapped the trained model into untrained LoRA weights. As a result, it seemed that the model has not trained at all.